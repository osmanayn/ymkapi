services:
  - type: web
    name: yemek-tarif-api
    runtime: python
    buildCommand: apt-get install -y chromium-driver && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
